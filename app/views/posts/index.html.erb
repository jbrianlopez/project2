
<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title></title>

  </head>
    <body>
      <div class = "container">
        <h1>Your Posts</h1>
          <table>
            <thead>
              <tr>
              <th>Title</th>
              <th>Body</th>

              <th colspan="3"></th>
              </tr>
            </thead>
            <tbody class = "posts">
              <% @posts.each do |post| %>
                <tr class = "post">
                  <td><%= post.title %></td>
                  <td><%= post.body %></td>
                    <% if current_user == post.user %>
                  <td><%= link_to 'Edit', edit_user_post_path(post.user, post) %></td>
                </tr>
                    <% end %>
              <% end %>
            </tbody>
          </table>
      </div>
    </body>
</html>
